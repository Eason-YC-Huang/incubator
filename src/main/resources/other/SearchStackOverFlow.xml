<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<executeUnitList>
    <executeUnitList>
        <executeUnitList version="0.1">
            <uuid>70e68ee3-320e-4ba7-b4b3-ab63bd01b5f7</uuid>
            <name>SearchStackOverflow</name>
            <desc>search selected text on stackoverflow</desc>
            <libPath></libPath>
            <className>com.github.hexffff0.incubator.other.stackoverflow.SearchStackOverflow</className>
            <sourceCode>package com.github.hexffff0.incubator.other.stackoverflow;

import java.util.Map;
import org.apache.commons.lang3.StringUtils;
import com.intellij.ide.BrowserUtil;
import com.intellij.openapi.actionSystem.AnActionEvent;
import com.intellij.openapi.actionSystem.LangDataKeys;
import com.intellij.openapi.editor.Caret;
import com.intellij.openapi.editor.CaretModel;
import com.intellij.openapi.editor.Editor;
public class SearchStackOverflow {

    public void main(Map&lt;String, Object&gt; context) {
        String stackOverFlow = "https://stackoverflow.com/search?q=";
        AnActionEvent e = (AnActionEvent) context.get("AnActionEvent");
        Editor editor = e.getData(LangDataKeys.EDITOR);
        if (editor == null) {
            return;
        }
        CaretModel caretModel = editor.getCaretModel();
        Caret currentCaret = caretModel.getCurrentCaret();
        String selectedText = currentCaret.getSelectedText();
        if (StringUtils.isNotBlank(selectedText)) {
            selectedText = selectedText.trim();
            BrowserUtil.browse(stackOverFlow + selectedText);
        } else {
            BrowserUtil.browse(stackOverFlow);
        }
    }

}
</sourceCode>
        </executeUnitList>
    </executeUnitList>
</executeUnitList>
