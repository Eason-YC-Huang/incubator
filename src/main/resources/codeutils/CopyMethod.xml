<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<executeUnitList>
    <executeUnitList>
        <executeUnitList version="0.1">
            <uuid>db75b345-5c5c-48c2-b3ff-c23bc0fef54a</uuid>
            <name>CopyMethod</name>
            <desc></desc>
            <libPath></libPath>
            <className>com.github.hexffff0.incubator.codeutils.copymethod.CopyMethod</className>
            <sourceCode>package com.github.hexffff0.incubator.codeutils.copymethod;

import java.util.List;
import java.util.Map;
import com.github.hexffff0.plugin.utils.JavaUtils;
import com.intellij.openapi.actionSystem.AnActionEvent;
import com.intellij.openapi.actionSystem.CommonDataKeys;
import com.intellij.openapi.editor.Editor;
import com.intellij.openapi.project.Project;
import com.intellij.psi.PsiClass;
import com.intellij.psi.PsiFile;
import com.intellij.psi.PsiMethod;
public class CopyMethod {

    public void main(Map&lt;String, Object&gt; context) {
        AnActionEvent event = (AnActionEvent) context.get("AnActionEvent");
        Project project = event.getProject();
        if (project == null) {
            return;
        }
        PsiClass selectedClass = JavaUtils.selectClass("select class", project);
        List&lt;PsiMethod&gt; methods = JavaUtils.selectMethods(selectedClass, "select method", true, true);
        Editor editor = event.getData(CommonDataKeys.EDITOR);
        if (editor == null) {
            return;
        }
        PsiFile file = event.getData(CommonDataKeys.PSI_FILE);
        methods.forEach(method -&gt; JavaUtils.writeToCaret(method.getText(), file, editor));
    }
}
</sourceCode>
        </executeUnitList>
    </executeUnitList>
</executeUnitList>
